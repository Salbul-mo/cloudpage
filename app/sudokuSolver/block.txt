
            <NumberBox
                key={`${0}-${0}-${0}`}
                id={`${0}-${0}-${0}`}
                box={0}
                row={0}
                col={0}
                value={totalNumber[0][0]}
                className={`sudokuBox border-l-tokyo_night-50 border-l-3 border-t-tokyo_night-50 border-t-3 groupBox-0 groupRow-0 groupCol-0`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${0}-${0}-${1}`}
                id={`${0}-${0}-${1}`}
                box={0}
                row={0}
                col={1}
                value={totalNumber[0][1]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-0 groupRow-0 groupCol-1`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${0}-${0}-${2}`}
                id={`${0}-${0}-${2}`}
                box={0}
                row={0}
                col={2}
                value={totalNumber[0][2]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-0 groupRow-0 groupCol-2`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${1}-${0}-${3}`}
                id={`${1}-${0}-${3}`}
                box={1}
                row={0}
                col={3}
                value={totalNumber[0][3]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-1 groupRow-0 groupCol-3`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${1}-${0}-${4}`}
                id={`${1}-${0}-${4}`}
                box={1}
                row={0}
                col={4}
                value={totalNumber[0][4]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-1 groupRow-0 groupCol-4`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${1}-${0}-${5}`}
                id={`${1}-${0}-${5}`}
                box={1}
                row={0}
                col={5}
                value={totalNumber[0][5]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-1 groupRow-0 groupCol-5`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${2}-${0}-${6}`}
                id={`${2}-${0}-${6}`}
                box={2}
                row={0}
                col={6}
                value={totalNumber[0][6]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-2 groupRow-0 groupCol-6`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${2}-${0}-${7}`}
                id={`${2}-${0}-${7}`}
                box={2}
                row={0}
                col={7}
                value={totalNumber[0][7]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-2 groupRow-0 groupCol-7`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${2}-${0}-${8}`}
                id={`${2}-${0}-${8}`}
                box={2}
                row={0}
                col={8}
                value={totalNumber[0][8]}
                className={`sudokuBox border-r-tokyo_night-50 border-r-3 border-t-tokyo_night-50 border-t-3 groupBox-2 groupRow-0 groupCol-8`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${0}-${1}-${0}`}
                id={`${0}-${1}-${0}`}
                box={0}
                row={0}
                col={0}
                value={totalNumber[1][0]}
                className={`sudokuBox border-l-tokyo_night-50 border-l-3 border-t-tokyo_night-50 border-t-3 groupBox-0 groupRow-0 groupCol-0`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${0}-${1}-${1}`}
                id={`${0}-${1}-${1}`}
                box={0}
                row={0}
                col={1}
                value={totalNumber[1][1]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-0 groupRow-0 groupCol-1`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${0}-${1}-${2}`}
                id={`${0}-${1}-${2}`}
                box={0}
                row={0}
                col={2}
                value={totalNumber[1][2]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-0 groupRow-0 groupCol-2`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${1}-${1}-${3}`}
                id={`${1}-${1}-${3}`}
                box={1}
                row={1}
                col={3}
                value={totalNumber[1][3]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-1 groupRow-0 groupCol-3`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${1}-${1}-${4}`}
                id={`${1}-${1}-${4}`}
                box={1}
                row={1}
                col={4}
                value={totalNumber[1][4]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-1 groupRow-0 groupCol-4`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${1}-${1}-${5}`}
                id={`${1}-${1}-${5}`}
                box={1}
                row={1}
                col={5}
                value={totalNumber[1][5]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-1 groupRow-0 groupCol-5`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${2}-${1}-${6}`}
                id={`${2}-${1}-${6}`}
                box={2}
                row={1}
                col={6}
                value={totalNumber[1][6]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-2 groupRow-0 groupCol-6`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${2}-${1}-${7}`}
                id={`${2}-${1}-${7}`}
                box={2}
                row={1}
                col={7}
                value={totalNumber[1][7]}
                className={`sudokuBox border-t-tokyo_night-50 border-t-3 groupBox-2 groupRow-0 groupCol-7`}
                onBoxClick={handleBoxClick}
            />
            <NumberBox
                key={`${2}-${1}-${8}`}
                id={`${2}-${1}-${8}`}
                box={2}
                row={1}
                col={8}
                value={totalNumber[1][8]}
                className={`sudokuBox border-r-tokyo_night-50 border-r-3 border-t-tokyo_night-50 border-t-3 groupBox-2 groupRow-0 groupCol-8`}
                onBoxClick={handleBoxClick}
            />
